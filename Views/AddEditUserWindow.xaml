<Window x:Class="RealEstateAgency.Views.AddEditUserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Добавить пользователя" Height="525" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Window.Resources>
        <Style x:Key="FormLabelStyle" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,10,0,5"/>
        </Style>

        <Style x:Key="FormTextBoxStyle" TargetType="TextBox">
            <Setter Property="Height" Value="30"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>

        <Style x:Key="FormComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Height" Value="30"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>

        <Style x:Key="SubmitButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#4CAF50"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
        </Style>

        <Style x:Key="CancelButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#9E9E9E"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="20,10"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                
                <TextBlock Text="Основная информация" FontWeight="Bold" FontSize="14" Margin="0,0,0,10"/>

                
                <TextBlock Text="ФИО *" Style="{StaticResource FormLabelStyle}"/>
                <TextBox Text="{Binding FullName, UpdateSourceTrigger=PropertyChanged}" 
                         Style="{StaticResource FormTextBoxStyle}"/>

              
                <TextBlock Text="Email *" Style="{StaticResource FormLabelStyle}"/>
                <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" 
                         Style="{StaticResource FormTextBoxStyle}"/>

                
                <TextBlock Text="Телефон" Style="{StaticResource FormLabelStyle}"/>
                <TextBox Text="{Binding PhoneNumber, UpdateSourceTrigger=PropertyChanged}" 
                         Style="{StaticResource FormTextBoxStyle}"/>

                <TextBlock Text="Паспорт (серия и номер)" Style="{StaticResource FormLabelStyle}"/>
                <TextBox Text="{Binding Passport, UpdateSourceTrigger=PropertyChanged}" 
         Style="{StaticResource FormTextBoxStyle}"
         ToolTip="В формате: 1234 567890"/>


                
                <TextBlock Text="Роль *" Style="{StaticResource FormLabelStyle}"/>
                <ComboBox ItemsSource="{Binding UserTypes}" 
                          DisplayMemberPath="Name"
                          SelectedValuePath="Id"
                          SelectedValue="{Binding UserTypeId, UpdateSourceTrigger=PropertyChanged}"
                          Style="{StaticResource FormComboBoxStyle}"/>


            </StackPanel>
        </ScrollViewer>

        
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="{Binding SubmitButtonText}" 
                    Style="{StaticResource SubmitButtonStyle}"
                    Command="{Binding SubmitCommand}"/>
            <Button Content="Отмена" 
                    Style="{StaticResource CancelButtonStyle}"
                    Command="{Binding CancelCommand}"/>
        </StackPanel>
    </Grid>
</Window>